# =============================================================================
# VereinsKasse - Umgebungsvariablen
# Kopiere diese Datei zu .env und trage deine Werte ein:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Datenbank (PostgreSQL)
# -----------------------------------------------------------------------------
DB_PASSWORD=sicheres_passwort_hier_eintragen

# -----------------------------------------------------------------------------
# Sicherheit
# Zufälligen Schlüssel generieren mit: openssl rand -hex 32
# -----------------------------------------------------------------------------
SECRET_KEY=hier_openssl_rand_hex_32_ausgabe_eintragen

# -----------------------------------------------------------------------------
# SMTP E-Mail (optional - für Willkommensmail, Mahnungen, etc.)
# Ohne SMTP funktioniert die App, nur keine E-Mails werden versendet.
# Beispiel für Gmail: smtp.gmail.com, Port 587
# Beispiel für Strato: smtp.strato.de, Port 587
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.dein-anbieter.de
SMTP_PORT=587
SMTP_USER=noreply@deine-domain.de
SMTP_PASSWORD=dein_smtp_passwort
SMTP_FROM=VereinsKasse <noreply@deine-domain.de>

# -----------------------------------------------------------------------------
# URLs
# NEXT_PUBLIC_API_URL: Leer lassen wenn nginx auf Port 80 läuft (Standard).
# Nur setzen wenn die API auf einem anderen Port/Host erreichbar ist.
# Beispiel: http://192.168.1.100:8000
# -----------------------------------------------------------------------------
FRONTEND_URL=http://localhost
NEXT_PUBLIC_API_URL=

# -----------------------------------------------------------------------------
# Server-Binding
# HOST_IP: IP-Adresse auf der nginx lauscht.
#   0.0.0.0 = alle Interfaces (Standard)
#   127.0.0.1 = nur lokal (hinter einem externen Reverse-Proxy)
# HTTP_PORT: Port für nginx (Standard: 80)
# -----------------------------------------------------------------------------
HOST_IP=0.0.0.0
HTTP_PORT=80

# -----------------------------------------------------------------------------
# Umgebung
# -----------------------------------------------------------------------------
ENVIRONMENT=production
